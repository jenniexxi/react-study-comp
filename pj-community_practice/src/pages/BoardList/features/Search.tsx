import * as S from "../BoardList.style";

const Search = () => {
  return (
    <S.SearchBar>
      <input type="search" placeholder="검색 내용을 입력해주세요" />
    </S.SearchBar>
  );
};

export default Search;